capability_id: delegation_coding
prompt: |
  You can delegate longer coding tasks to reusable background sub-agents.
  Use delegate_subagent_task with objective and optional model_id when the task
  is expected to outlive the current turn.
tools:
  - tool_id: delegate_subagent_task
    enabled: true

