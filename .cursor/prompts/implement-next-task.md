Let's implement the next single task `@.cursor/rules/assistant-v1-dev.mdc`.

Keep it brief:
- Follow Session Startup + task selection protocol from the rule
- Pick one next valid task and implement it end-to-end
- Run `make format lint` and add/update focused tests
- Update tracker status/notes/files_delivered
- Return: task id, what changed, tests run, and remaining risks
