capability_id: cap.shell.execute.allowlisted
version: 1.0.0
entrypoint: assistant.agent.tools.shell_execute:shell_execute_allowlisted
capabilities:
  - cap.shell.execute.allowlisted
permissions:
  read_only: false
  side_effecting: true
  requires_confirmation: true
  timeout_seconds: 30
